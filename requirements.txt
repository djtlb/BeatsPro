# Smart Music Generator AI - Optimized Dependencies
# Version-pinned for maximum compatibility

# Core web framework
flask==2.3.3
werkzeug==2.3.7

# Scientific computing (version-pinned for stability)
numpy==1.24.3

# MIDI processing (lightweight and reliable)
pretty_midi==0.2.10
mido==1.3.0

# AI/ML framework (optional - install separately if this fails)
tensorflow==2.13.0

# Audio processing (optional)
scipy==1.11.4
librosa==0.10.1

# Utilities
music21==9.1.0
