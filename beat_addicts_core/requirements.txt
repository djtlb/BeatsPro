# 🎵 BEAT ADDICTS v2.0 - Minimal Self-Contained Dependencies
# Only essential packages with pre-cross referenced compatibility

# Core web framework for BEAT ADDICTS Studio (REQUIRED)
flask==3.0.0
werkzeug==3.0.1
jinja2==3.1.2

# BEAT ADDICTS specific optimizations (REQUIRED)
colorama==0.4.6          # For colored terminal output
tqdm==4.66.1             # Progress bars for generation

# Self-contained - no weak external dependencies
# All other functionality is built-in to BEAT ADDICTS modules
